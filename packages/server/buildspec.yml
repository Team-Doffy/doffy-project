version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - cd doffy-project/packages/server
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo $(basename ./build/libs/*.jar)
artifacts:
  files:
    - doffy-project/packages/server/build/libs/*.jar
    - doffy-project/packages/server/scripts/**
    - doffy-project/packages/server/appspec.yml
  discard-paths: yes